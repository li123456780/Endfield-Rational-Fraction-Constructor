# 完整证明：仅用 $\boldsymbol{\frac12、\frac13}$ 分流器和汇流器可构造全部正有理数分流器

## 前置定义与基础性质

### 1. 分流器运算规则

分流器两种基础组合（对应电路串并联）：

1. **串联（乘法）**：分流系数 $a$ 串联 $b$，总系数 $a\cdot b$；
2. **并联（加法）**：分流系数 $a$ 并联 $b$并将输出连接汇流器，总系数 $a+b$；
   已知基底：$s_1=\dfrac12,\ s_2=\dfrac13$，所有可构造分流系数集合记为 $S$。

### 2. 简化核心目标

任一个正有理数可写为 $\dfrac{n}{m}\ (n,m\in\mathbb{N}^*)$。
若能构造单位分数 $\dfrac1m$，将 $n$ 个 $\dfrac1m$ **并联**，即可得到：

$$
\underbrace{\frac1m+\frac1m+\dots+\frac1m}_{n\text{个}}=\frac{n}{m}
$$

因此只需证明：**对任意正整数 $k$，$\dfrac1k\in S$**。

---

## 引理1：递推构造公式（反馈回路）

构造带反馈的分流拓扑：输入固定为 $1$，系统输出 $x$ 引回输入端并联，拓扑满足方程：

$$
1\cdot \frac1k + x\cdot \frac1k = x
$$

整理：

$$
\frac{1+x}{k}=x \implies 1+x=kx \implies x=\frac{1}{k-1}
$$

**引理结论**：若 $\dfrac1k\in S$，则必有 $\dfrac{1}{k-1}\in S$。
反向等价表述：若能构造 $\dfrac{1}{t+1}$，就能构造 $\dfrac1t$。

---

## 引理2：对任意质数 $p$，$\dfrac1p$ 可由更小可构造分数生成

设 $p$ 为奇质数，则 $p+1$ 是偶数，$\dfrac{p+1}{2}=t\in\mathbb{N}^*$，变形：

$$
\frac1p = \frac{2}{p+1} = \frac{1}{t}\cdot \frac12
$$

由串联规则：只要 $\dfrac{1}{t}\in S$，乘基底 $\dfrac12$ 就得到 $\dfrac2{p+1}=\dfrac1p$。
即：**构造 $\dfrac1p$ 等价于构造 $\dfrac{1}{t},\ t=\dfrac{p+1}{2}<p$**。

### 数学归纳逻辑（质数从小到大）

- 基例：最小质数 $2,3$，$\dfrac12,\dfrac13$ 本身是基底，显然属于 $S$；
- 归纳假设：对所有小于质数 $p$ 的正整数 $t$，都有 $\dfrac1t\in S$；
- 归纳步骤：$t=\dfrac{p+1}{2}<p$，由假设 $\dfrac1t\in S$，串联 $\dfrac12$ 得 $\dfrac1t\cdot\dfrac12=\dfrac2{p+1}=\dfrac1p$，故 $\dfrac1p\in S$。

由数学归纳法，**所有质数分母的单位分数 $\dfrac1p\in S$**。

### 实例验证（原文举例）

1. $p=5$：$t=\dfrac{5+1}{2}=3$，$\dfrac13\in S$，$\dfrac13\cdot\dfrac12=\dfrac16 \implies$ 由引理1，$\dfrac1{6-1}=\dfrac15\in S$；
2. $p=7$：$t=\dfrac{7+1}{2}=4$，先由 $\dfrac13\cdot\dfrac12\cdot\dfrac12=\dfrac1{12}\implies\dfrac1{11}$，$\dfrac12\cdot\dfrac12=\dfrac14$，再由 $\dfrac14$ 反馈得 $\dfrac17$；
3. $p=11$：$t=\dfrac{11+1}{2}=6$，$\dfrac16=\dfrac12\cdot\dfrac13\in S$，反馈得 $\dfrac1{11}$；
   所有奇质数均可按此方式递推构造。

---

## 引理3：所有合数分母的单位分数 $\dfrac1m\in S$

对任意合数 $m>1$，质因数分解 $m=p_1^{a_1}p_2^{a_2}\dots p_r^{a_r}$。
由串联规则：

$$
\frac1m = \underbrace{\frac1{p_1}\cdot\frac1{p_1}\dots}_{a_1\text{次}} \cdot \underbrace{\frac1{p_2}\cdot\frac1{p_2}\dots}_{a_2\text{次}} \dots
$$

已经证明所有质数倒数可构造，多次串联后得到合数倒数 $\dfrac1m\in S$。

### 举例

$m=8=2^3$：$\dfrac12\cdot\dfrac12\cdot\dfrac12=\dfrac18$；
$m=12=2^2\cdot3$：$\dfrac12\cdot\dfrac12\cdot\dfrac13=\dfrac1{12}$。

---

## 最终完整结论

1. 对任意正整数 $m$，无论 $m$ 是质数还是合数，单位分数 $\dfrac1m$ 均可通过 $\dfrac12,\dfrac13$ 的串并联+反馈拓扑构造；
2. 对任意正有理数 $\dfrac{n}{m}$，将 $n$ 个 $\dfrac1m$ 并联相加，得到 $\dfrac{n}{m}$；

因此：**仅使用分流系数 $\boldsymbol{\dfrac12、\dfrac13}$ 的串并联、反馈组合，可以构造出任意正有理数分流器**。

以质数 $p=100003$（十万级五位数质数）完整演示构造流程：

### 步骤1：转化递推关系

$p=100003$ 是奇质数，因此

$$
\frac1{100003} = \frac{2}{100003+1} = \frac{2}{100004} = \frac{1}{50002} \cdot \frac12
$$

只需先构造 $\displaystyle\frac{1}{50002}$，串联基底 $\frac12$ 得到 $\displaystyle\frac{1}{50002\times 2}=\frac{1}{100004}$，再用反馈引理：若有 $\frac1k$ 则可得 $\frac1{k-1}$，令 $k=100004$，即输出 $x=\frac{1}{100004-1}=\frac{1}{100003}$。

### 步骤2：分解 $50002$ 质因数

$$
50002 = 2 \times 23 \times 1087
$$

由串联乘法规则：

$$
\frac{1}{50002} = \frac12 \cdot \frac1{23} \cdot \frac1{1087}
$$

其中 $\frac12$ 是基底，只需分别构造 $\frac1{23},\frac1{1087}$。

### 步骤3：构造 $\frac1{23}$

$$
\frac1{23}=\frac{2}{24}=\frac1{12}\cdot\frac12,\quad \frac1{12}=\frac12\cdot\frac12\cdot\frac13
$$

1. 串三个基底：$\frac12\cdot\frac12\cdot\frac13=\frac1{12}$
2. 再串 $\frac12$ 得到 $\frac1{24}$
3. 反馈回路：$k=24\Rightarrow x=\frac1{24-1}=\frac1{23}$

### 步骤4：构造 $\frac1{1087}$（质数）

$$
\frac1{1087}=\frac{2}{1088}=\frac1{544}\cdot\frac12
$$

$$
544=2^5 \times 17,\quad \frac1{544}=\underbrace{\frac12\cdot\frac12\cdot\frac12\cdot\frac12\cdot\frac12}_{5个\frac12} \cdot \frac1{17}
$$

$\frac1{17}$ 同理递归分解：

$$
\frac1{17}=\frac{2}{18}=\frac19\cdot\frac12,\quad \frac19=\frac13\cdot\frac13
$$

全部基底仅 $\frac12,\frac13$，逐层串联、反馈即可得到 $\frac1{1087}$。

### 步骤5：合并得到 $\frac{1}{100003}$

1. 串联基底得到 $\frac12,\frac13$ 的组合 $\frac1{50002}$；
2. 再串联 $\frac12$ 得到 $\frac1{100004}$；
3. 搭建反馈分流拓扑（输入1，输出回并输入端），输入系数 $\frac1{100004}$，解方程得输出 $x=\frac{1}{100003}$。

### 拓展：任意五位数有理数示例

取有理数 $\displaystyle\frac{72}{100003}$：
构造出 $\frac1{100003}$ 后，将72路 $\frac1{100003}$ 分流器**并联**，相加得到：

$$
\underbrace{\frac{1}{100003}+\dots+\frac{1}{100003}}_{72项}=\frac{72}{100003}
$$

完全仅依靠 $\frac12,\frac13$ 的串并联+反馈拓扑实现。

# 简化核心思路

前面已经证明：对任意正整数 $m$，单位分数 $\dfrac1m$ 可仅用 $\dfrac12,\dfrac13$ 分流器构造。
朴素方案：要得到 $\dfrac{h}{m}$，直接并联 $h$ 路 $\dfrac1m$ 分流器；但当分子 $h$ 很大时，支路过多、拓扑冗余。
因此提出优化思路：利用**分母因数拆分分子**，把大分子拆成小数目的可构造分式之和，减少并联支路。

### 拆分原理

设目标分数 $\dfrac{n}{m}$，对分子 $n$ 做线性拆分：

$$
n = a_1 k_1 + a_2 k_2 + \dots + a_t k_t
$$

其中 $k_1,k_2,\dots,k_t$ 是 $m$ 的真因数（满足 $k_i<m$），两边同除以 $m$：

$$
\frac{n}{m} = a_1\cdot\frac{k_1}{m} + a_2\cdot\frac{k_2}{m} + \dots + a_t\cdot\frac{k_t}{m}
$$

每一项 $\dfrac{k_i}{m}$ 的分母更小，可复用之前已构造完成的分流拓扑，不用从零搭建。

### 实例：$\boldsymbol{\dfrac{7}{15}}$

1. 分母 $m=15$，其正因数：$1,3,5,15$，取真因数 $3,5$；
2. 分子拆分：$7 = 2\times 3 + 1\times 1$，等价 $7=2\times3 +1$；
3. 分式变形：

$$
\frac{7}{15} = \frac{2\times 3}{15} + \frac{1}{15} = \frac{2}{5} + \frac{1}{15}
$$

4. 分项构造：
   - $\dfrac15$：之前递推可构造，2路 $\dfrac15$ 并联得到 $\dfrac25$；
   - $\dfrac1{15}=\dfrac13\cdot\dfrac15$：$\dfrac12,\dfrac13$ 串联组合直接得到；
5. 拓扑实现：一路 $\dfrac25$、一路 $\dfrac1{15}$ 并联汇总，仅2条输出支路，远优于直接7路 $\dfrac1{15}$ 并联。

---

## 二、数学严谨性补充（解决拆分可行性）

### 1. 拆分一定存在（贝祖定理/整数分拆）

对任意 $n,m\in\mathbb{N}^*$，设 $d=\gcd(n,m)$，$d\mid m$，因此 $d$ 是 $m$ 的因数。
存在整数 $s,t$ 使得 $sn+tm=d$，进一步可将 $n$ 表为 $m$ 的若干因数的整数线性组合，保证拆分恒成立。

### 2. 拓扑复用优势

- 朴素方案：构造 $\dfrac{n}{m}$ 需要 $n$ 条完全相同的 $\dfrac1m$ 支路，$n$ 为五位数时拓扑规模爆炸；
- 因数拆分方案：仅需少量不同小分母分式并联，复用已搭建的小分母分流模块，大幅减少器件数量。

# 分流器分子化简贪心算法（快速近似算法）

## 一、算法核心目标

给定分数 $\dfrac{n}{m}$，不直接并联 $n$ 条 $\dfrac{1}{m}$ 支路（分子极大时支路爆炸），采用**贪心因数分解**拆分分子，复用已构造的小分母分流模块，大幅减少硬件支路数量。
前置基础：已证明任意单位分数 $\dfrac{1}{k}$ 可仅用 $\dfrac12、\dfrac13$ 串并联+反馈构造。

## 二、分步算法流程

### 步骤1：提取分母全部因数并排序

1. 求出 $m$ 的所有正因数；
2. 从大到小排序得到序列：$a_1,a_2,\dots,a_n$。

### 步骤2：贪心拆分分子 $n$

1. 取当前剩余分子 $R$，初始 $R=n$；
2. 选序列里满足 $k_1 a_1 < R$ 的最大整数系数 $k_1$，记剩余 $R_1=R-k_1 a_1$；
3. 对 $R_1$，选满足 $k_2 a_2 < R_1$ 的最大整数 $k_2$，剩余 $R_2=R_1-k_2 a_2$；
4. 循环迭代，直到剩余值 $R_t=0$；
5. 最终得到分子分解式：

$$
n = k_1 a_1 + k_2 a_2 + \dots + k_t a_t
$$

两边同除 $m$：

$$
\frac{n}{m} = k_1\cdot\frac{a_1}{m} + k_2\cdot\frac{a_2}{m} + \dots + k_t\cdot\frac{a_t}{m}
$$

### 步骤3：分流拓扑搭建规则

1. 预构造所有单位分数 $\dfrac{1}{m},\dfrac{1}{a_1},\dfrac{1}{a_2}\dots$，并对每条分流输出做数值标记；
2. 对每一项 $k_i\cdot\dfrac{a_i}{m}=k_i\cdot\dfrac{1}{m/a_i}$：
   - 若已有现成 $\dfrac{1}{m/a_i}$ 分流模块，直接并联 $k_i$ 路该模块，接入总汇合器；
   - 若模块不存在：取 $z_i=\dfrac{m}{a_i}$（$z_i$ 是 $m$ 的因数），递归构造 $\dfrac{1}{z_i}$ 分流结构，再并联 $k_i$ 路；
3. 所有分项支路汇总到同一汇合输出端，得到最终 $\dfrac{n}{m}$
   > 注: 若当前需要 $k_n$ 路 $\dfrac{1}{z_n}$，但系统中只预制了少量该模块，则递归构造更多 $\dfrac{1}{z_n}$ 单元补足数量，再并联接入总汇合端。
   >

### 步骤4：算法最优约束

若预先限定输入分数满足 $n<m$（真分数），该算法输出具体的步骤流程可以在那一点里面看到的拓扑支路数达到理论最少值，全程仅复用由 $\dfrac12、\dfrac13$ 生成的基础分流单元。

---

## 三、实例演算（沿用前文 $\boldsymbol{\dfrac{7}{15}}$）

1. $m=15$，全部因数：$1,3,5,15$，从大到小排序 $[5,3,1]$；
2. 初始剩余 $R=7$：
   - 最大因数 $a_1=5$，取最大 $k_1=1$（$1\times5<7$），剩余 $R_1=7-5=2$；
   - 下一个因数 $a_2=3$，$3>2$ 无法选取；
   - 下一个因数 $a_3=1$，取 $k_2=2$，剩余 $R_2=0$；
3. 拆分结果：$7=1\times5 + 2\times1$；

$$
\frac{7}{15}=\frac{5}{15}+\frac{2}{15}=\frac13 + 2\cdot\frac{1}{15}
$$

4. 拓扑实现：
   - $\dfrac13$ 是原生基底，1路直接接入；
   - $\dfrac{1}{15}=\dfrac12\cdot\dfrac13\cdot\dfrac15$，搭建后并联2路；
   - 两路汇总输出；仅3条支路，远优于直接7路 $\dfrac{1}{15}$ 并联。

